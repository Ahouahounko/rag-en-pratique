# Choisir un fournisseur depuis la racine du dépôt
pip install -e ".[openai]"
pip install -e ".[huggingface]"

# Ollama utilise la bibliothèque standard Python ; installer puis démarrer Ollama séparément
pip install -e .

# Avec lecture PDF
pip install -e ".[pdf]"

# Avec API FastAPI et interface Streamlit
pip install -e ".[app]"

# Installation complète
pip install -e ".[all]"
